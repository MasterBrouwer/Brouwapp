import React from 'react';

const RecipeEditor: React.FC = () => {
  return (
    <div className="recipe-editor">
      {/* TODO: Render recipe editor with 4 sections */}
      {/* 1. General data */}
      {/* 2. Ingredients */}
      {/* 3. Brew process */}
      {/* 4. Expected results */}
    </div>
  );
};

export default RecipeEditor;
