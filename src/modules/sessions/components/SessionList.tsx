import React from 'react';

const SessionList: React.FC = () => {
  return (
    <div className="session-list">
      {/* TODO: Render active, paused, and completed sessions */}
    </div>
  );
};

export default SessionList;
