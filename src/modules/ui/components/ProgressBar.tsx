import React from 'react';

const ProgressBar: React.FC = () => {
  return (
    <div className="progress-bar">
      {/* TODO: Render progress bar for session steps */}
    </div>
  );
};

export default ProgressBar;
