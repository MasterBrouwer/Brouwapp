import React from 'react';

const SearchBar: React.FC = () => {
  return (
    <div className="search-bar">
      {/* TODO: Render search bar component */}
    </div>
  );
};

export default SearchBar;
